apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: community-backend
spec:
  type: ClusterIP
  selector:
    app: community-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
